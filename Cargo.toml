[package]
name = "cnmd"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
oxichrome = { version = "0.1" }
wasm-bindgen = "0.2"
serde = { version = "1", features = ["derive"] }
leptos = { version = "0.8.19", features = ["csr"] }
